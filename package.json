{
  "name": "language",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm --prefix web run dev",
    "web:dev": "npm --prefix web run dev",
    "web:build": "npm --prefix web run build",
    "web:start": "npm --prefix web run start",
    "cli:legacy": "node cli.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "openai": "^4.0.0"
  }
}
